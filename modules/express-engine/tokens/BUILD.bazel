package(default_visibility = ["//modules/express-engine:__subpackages__"])

load("//tools:defaults.bzl", "ts_library", "ng_module", "ng_package")

exports_files(["package.json"])

ng_module(
    name = "tokens",
    srcs = glob([
        "*.ts",
        "src/*.ts"
    ]),
)
